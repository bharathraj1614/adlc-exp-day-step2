# Spec

<!-- Business layer + Known Constraints — carried through exactly as given, not written by you -->

**Title:** Real-Time Currency Conversion & Audit Trail

**Business Idea:** Treasury operations teams at our enterprise customers currently convert currency using manual lookups against a third-party portal — slow, and it leaves no record for compliance. We need instant, self-serve conversion inside our own app, and because these are regulated transactions, every conversion must be reconstructable for an auditor on demand, not pieced together afterward from emails or spreadsheets.

**Persona:** Treasury operations analysts at enterprise customers, who process multiple cross-border settlements a day and are personally accountable if a conversion can't be justified during an audit.

**User Need:** See a trustworthy converted amount the moment I enter it — and be able to pull up any past conversion, with its rate and timestamp, the moment an auditor asks for it.

**Known Constraints:**
- Frontend must read `VITE_API_URL` dynamically at container runtime using
  a generic entrypoint script placeholder replacement; replace the placeholder token
  `__VITE_API_URL__` inside the built `index.html` (or equivalent) with the runtime
  `VITE_API_URL` value.
- Backend must read database configurations exclusively from runtime
  environment variables.
- Backend must query external currency conversion data exclusively using a base URL from an environment variable (`CURRENCY_API_BASE_URL`), defaulting to an unauthenticated open-source endpoint like Frankfurter (`https://frankfurter.dev`).
- The application must not hardcode third-party API response schemas; utilize flexible JSON property mapping or an abstraction layer to isolate internal compliance tracking from external JSON structure changes (such as `rates` vs `conversion_rates`).
- The audit trail record must capture the exact execution timestamp generated by your backend server alongside any date or sequence markers returned by the external provider, ensuring complete sub-second audit reconstruction even if the provider only exposes a calendar date.
- External HTTP calls must handle failures gracefully with fallback values or clear domain exceptions rather than bubbling up raw network or serialization errors.
- CI/CD pipeline must inject placeholders safely at deployment time without
  exposing sensitive data in container layers.
- Restricted to specific stack versions: React (Vite/Node 24.*) and C#
  (**backend must target `net10.0` for build/test; CI must use `dotnet-version: 10.0.x`**).
- Must adhere to a strict folder layout (`src/frontend` and `src/backend`).
- A .NET solution file must always be generated at `src/backend/OuterloopLabApi.sln` via `dotnet new sln` + `dotnet sln add`, with the backend project added to it, and CI/build/test commands must invoke this `.sln` rather than the `.csproj` directly.
- Deploy as a single Azure Container App using the sidecar pattern to host
  both frontend and backend.
- Authenticate to Cosmos DB exclusively using token-based authentication via the pre-assigned User-Assigned Managed Identity, instantiated using `DefaultAzureCredential` configured with the specific `ManagedIdentityClientId`. Absolutely zero connection strings, master keys, or account keys are permitted in code, configurations, or logs.
- Utilize `Microsoft.Azure.Cosmos` and `Azure.Identity` packages for
  secure, token-based database interactions, including data-plane CRUD operations like adding and deleting items.
- Include `Azure.ResourceManager` and `Azure.ResourceManager.CosmosDB` packages to programmatically provision the Cosmos DB database and container via the control plane inside the application startup lifecycle (`Program.cs`) before the web application runs, utilizing the same token-based Managed Identity credentials for authentication.
  - ARM provisioning is best-effort; Managed Identity RBAC for ARM may differ from data-plane RBAC.
  - CRUD (and data-plane creation if needed) must be done via the Cosmos SDK (data-plane) using token-based Managed Identity credentials; access keys must not be used.
  - After ARM attempt, startup must run token-authenticated data-plane `CreateDatabaseIfNotExistsAsync` and `CreateContainerIfNotExistsAsync` and only proceed if the database/container are available (either already exist or were created). If token-authenticated data-plane create-if-not-exists fails, startup must fail.
- Map and bind all configuration values, including `AZURE_MANAGED_IDENTITY_CLIENT_ID`, endpoint URLs, and resource names, directly from the environment variables **using the exact keys** defined in `docs\CONTAINER_ENVIRONMENT_VARIABLES.md` without using local settings configuration files as fallbacks.

<!-- Technical layer — this is what you add -->

**Current Context:** Assumption, stated explicitly because no repository inspection is allowed: the implementation should create or update `src/backend/OuterloopLabApi.sln`, `src/backend/OuterloopLabApi.csproj`, `src/backend/Program.cs`, `src/backend/Controllers/ConversionsController.cs`, `src/backend/Models/ConvertCurrencyRequest.cs`, `src/backend/Models/ConversionResponse.cs`, `src/backend/Models/ConversionAuditRecord.cs`, `src/backend/Services/ExternalRateProviderClient.cs`, `src/backend/Services/CurrencyConversionService.cs`, `src/backend/Services/ConversionAuditRepository.cs`, `src/frontend/index.html`, `src/frontend/package.json`, `src/frontend/src/main.jsx`, `src/frontend/src/App.jsx`, `src/frontend/src/components/ConversionForm.jsx`, `src/frontend/src/components/AuditLookup.jsx`, and `src/frontend/entrypoint.sh`.

**In Scope:**
- Add a backend API endpoint that accepts an amount, source currency, and target currency, fetches the current conversion rate from the configured external provider, computes the converted amount, and returns a response suitable for immediate UI display.
- Persist one immutable audit record per successful conversion in Cosmos DB, including request values, normalized rate data, provider date/sequence metadata if present, and the exact backend-generated execution timestamp.
- Add a backend API endpoint that retrieves a prior conversion audit record by a server-generated conversion identifier so an auditor-facing lookup can reconstruct the original result.
- Add frontend UI in `src/frontend` for entering conversion inputs, displaying the converted result and returned audit identifier, and looking up a prior conversion by identifier.
- Wire the frontend API client to the runtime-injected `VITE_API_URL` value so the same built frontend artifact can call the correct backend URL after deployment.
- Add automated tests for conversion input validation, provider response normalization, and graceful upstream failure handling.

**Out of Scope:**
- Bulk or batch currency conversion workflows.
- Manual rate override, approval flows, or editing previously stored audit records.
- Exporting audit records to CSV, PDF, email, or external compliance systems.
- Historical charting, trend analysis, or rate caching beyond the single live lookup needed for each conversion.
- Authentication, authorization, or persona management beyond what already exists in the host application/platform.

**Feature Constraints:**
- Use `IHttpClientFactory` for all outbound currency provider calls and isolate provider parsing behind a dedicated client/service boundary so the controller never depends on raw provider JSON.
- Validate that `amount` is greater than `0` and that `fromCurrency` and `toCurrency` are 3-letter ISO-style uppercase codes before any outbound call; invalid input must return HTTP 400 with `ProblemDetails`.
- Generate the conversion identifier on the server as a GUID string and use it as the primary lookup key returned to the frontend and stored with the audit record.
- Normalize provider data into one internal model that always contains `rate`, `providerDate`, `providerSequenceMarker`, and `executedAtUtc`, with `providerSequenceMarker` allowed to be `null` when the upstream provider does not supply one.
- Return graceful domain-level failures for provider unavailability or unparseable provider payloads as HTTP 502 with `ProblemDetails`, and do not expose raw exception messages, stack traces, or third-party payloads to the client.
- Use `decimal` for monetary calculation and persist both the original input amount and the exact rate used for the conversion; API responses should return the converted amount rounded consistently to 4 decimal places.

**Expected Outcome:** A user can submit `POST /api/conversions` with a payload such as `{ "amount": 100.00, "fromCurrency": "USD", "toCurrency": "EUR" }` and receive HTTP 200 with a response containing a server-generated `conversionId`, the original request values, a normalized `rate` (for example `0.92`), a computed `convertedAmount` (for example `92.0000`), the provider's effective date if supplied (for example `2026-08-01`), a nullable provider sequence marker, and an `executedAtUtc` timestamp such as `2026-08-01T12:34:56.789Z`. A subsequent `GET /api/conversions/{conversionId}` returns the same conversion details and audit metadata from Cosmos DB without recomputing the rate. If the provider call fails, times out, or returns a shape that cannot be normalized, the conversion endpoint returns HTTP 502 with a `ProblemDetails` body and no raw network/serialization exception text. If the user submits `{ "amount": 0, "fromCurrency": "US", "toCurrency": "EURO" }`, the API returns HTTP 400 with field-level validation details. In the deployed frontend, a conversion result and its audit identifier are visible immediately after submission, and the built `src/frontend/index.html` no longer contains the literal token `__VITE_API_URL__` after the runtime entrypoint replacement executes.

**Expected Agent Output:** OpenCode should produce one implementation pass that updates or creates only the files required under `src/backend` and `src/frontend`, including the `.sln`, backend startup/configuration, one conversion write endpoint, one audit lookup endpoint, the Cosmos-backed audit persistence layer, frontend conversion and lookup UI, runtime API URL placeholder replacement support, and automated tests for validation and provider-failure behavior. The final implementation report should list the exact files changed and the exact verification commands run, including `dotnet test src/backend/OuterloopLabApi.sln` and the frontend build command executed from `src/frontend`.

**Suggested Intent For OpenCode:** Implement the feature exactly as specified in `spec/real-time-currency-conversion-audit-trail.md`: add a live currency conversion flow plus immutable audit lookup across `src/backend` and `src/frontend`, and explicitly respect every Known Constraint in this spec, including `net10.0`, the required `src/backend/OuterloopLabApi.sln`, runtime-only environment binding with the exact documented keys, runtime replacement of `__VITE_API_URL__`, token-authenticated Cosmos access and startup provisioning behavior, the sidecar deployment shape, and graceful external HTTP failure handling; use `IHttpClientFactory`, normalize provider payloads behind an abstraction, return `ProblemDetails` for invalid/upstream-failure cases, persist backend-generated execution timestamps with provider metadata, and verify the result with backend tests plus a frontend build.
